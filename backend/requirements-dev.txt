# Зависимости только для запуска тестов и линтинга — в production-образ не входят.
-r requirements.txt

# Сам пакет: тесты импортируют elou_tutor, а не файлы по путям
-e .

# Запуск тестов
pytest>=8.0.0
# Нужен starlette.testclient (TestClient) в backend/tests/test_tutor.py
httpx>=0.27.0

# Линтер Python (набор правил задан в [tool.ruff] в pyproject.toml)
ruff>=0.14.0

# Проверка архитектурных контрактов слоёв ([tool.importlinter] в pyproject.toml)
import-linter>=2.0

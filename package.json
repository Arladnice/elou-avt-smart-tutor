{
  "name": "elou-avt-smart-tutor-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "python -m uvicorn elou_tutor.api.main:app --host 127.0.0.1 --port 8000 --reload",
    "frontend": "npm run dev --prefix frontend",
    "dev": "npx -y concurrently --kill-others \"npm run backend\" \"npm run frontend\""
  }
}

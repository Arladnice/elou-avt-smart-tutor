{
  "name": "elou-avt-smart-tutor-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "python -m uvicorn backend.main:app --host 127.0.0.1 --port 8000",
    "frontend": "npm run dev --prefix frontend",
    "dev": "npx -y concurrently \"npm run backend\" \"npm run frontend\""
  }
}
